import { domAttr } from 'portefeuille/helpers/dom-attr';
import { module, test } from 'qunit';

module('Unit | Helper | dom attr');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = domAttr([42]);
  assert.ok(result);
});
